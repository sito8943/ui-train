// emotion
import { css } from "@emotion/css";

const span = css({
  color: "#9f9d9d",
  marginTop: -20,
  marginBottom: 10,
  cursor: "pointer",
});

const titleArea = css({
  width: "85%",
  margin: "auto",
});

const titleCss = css({
  width: "70%",
});

const spanTitle = css({
  color: "#bdbbbb",
});

const centerRow = css({
  display: "flex",
  justifyContent: "center",
});

export { span, titleArea, titleCss, spanTitle, centerRow };
