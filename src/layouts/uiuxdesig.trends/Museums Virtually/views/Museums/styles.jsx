// emotion
import { css } from "@emotion/css";

const titleArea = css({
  marginTop: 20,
  width: "80%",
});

const starSpan = css({
  color: "#626262",
  display: "flex",
  cursor: "pointer",
});

const titleCss = css({
  color: "#1d1d1d",
});

export { starSpan, titleCss, titleArea };
